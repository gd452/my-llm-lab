# .envrc (프로젝트 루트)
if [ ! -d ".venv" ]; then
  echo "[direnv] creating .venv via uv..."
  uv venv .venv
  # pip 보강 (uv venv는 pip가 없을 수 있어요)
  . .venv/bin/activate
  uv pip install --upgrade pip
fi

# 핵심: uv의 .venv만 활성화
source .venv/bin/activate
